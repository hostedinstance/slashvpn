# 🔐 SlashVPN - Личный Кабинет

Полностью рабочий личный кабинет с безопасной регистрацией по email + код верификации.

## ✨ Возможности

- ✅ **Регистрация по email** - безопасная регистрация с подтверждением
- ✅ **Passwordless вход** - вход по email + 6-значный код
- ✅ **Верификация email** - подтверждение через код из письма
- ✅ **Личный кабинет** - защищенная страница с информацией о пользователе
- ✅ **Безопасность 2026** - современные стандарты защиты данных
- ✅ **Rate limiting** - защита от брутфорса
- ✅ **Логирование безопасности** - аудит всех действий
- ✅ **Красивый UI** - современный темный интерфейс

## 🛠 Технологии

- **Frontend**: Next.js 14 (App Router), React 19, TypeScript
- **Styling**: TailwindCSS, Framer Motion
- **Backend**: PocketBase (легкий SQLite бэкенд)
- **Email**: Resend API
- **Auth**: Passwordless (email + код)

## 📋 Требования

- Node.js 18+ или Bun
- PocketBase 0.21+
- Аккаунт Resend (бесплатно 100 писем/день)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Используя npm
npm install

# Или используя yarn
yarn install

# Или используя bun (быстрее)
bun install
```

### 2. Настройка PocketBase

#### Скачайте PocketBase:

```bash
# Linux/Mac
wget https://github.com/pocketbase/pocketbase/releases/download/v0.21.5/pocketbase_0.21.5_linux_amd64.zip
unzip pocketbase_0.21.5_linux_amd64.zip

# Windows - скачайте с GitHub Releases
```

#### Запустите PocketBase:

```bash
./pocketbase serve
```

PocketBase будет доступен по адресу: `http://127.0.0.1:8090`

#### Создайте админ аккаунт:

1. Откройте `http://127.0.0.1:8090/_/`
2. Создайте админ аккаунт

#### Создайте коллекции:

**Откройте админ панель PocketBase и создайте следующие коллекции:**

---

### 📊 Коллекция: `users` (уже есть по умолчанию)

Убедитесь что включены поля:
- `email` (email, required, unique)
- `username` (text, required, unique)
- `name` (text)
- `verified` (bool)
- `emailVisibility` (bool)
- `password` (password, min 8 символов)

**Добавьте дополнительные поля:**
- `registrationIp` (text, optional) - для логирования IP при регистрации

---

### 📊 Коллекция: `email_verifications`

Создайте новую коллекцию со следующими полями:

| Поле | Тип | Настройки |
|------|-----|-----------|
| `email` | email | required |
| `codeHash` | text | required |
| `attempts` | number | default: 0 |
| `maxAttempts` | number | default: 5 |
| `blocked` | bool | default: false |
| `blockedUntil` | date | optional |
| `expiresAt` | date | required |
| `verified` | bool | default: false |
| `ipAddress` | text | optional |
| `userAgent` | text | optional |

**API Rules:**
- List: только владелец или админ
- View: только владелец или админ
- Create: разрешить всем
- Update: только владелец или админ
- Delete: только админ

---

### 📊 Коллекция: `login_codes`

Для passwordless входа:

| Поле | Тип | Настройки |
|------|-----|-----------|
| `email` | email | required |
| `codeHash` | text | required |
| `attempts` | number | default: 0 |
| `maxAttempts` | number | default: 5 |
| `blocked` | bool | default: false |
| `expiresAt` | date | required |
| `used` | bool | default: false |

**API Rules:**
- List: только админ
- View: только админ
- Create: разрешить всем
- Update: только админ
- Delete: только админ

---

### 📊 Коллекция: `security_logs`

Для аудита безопасности:

| Поле | Тип | Настройки |
|------|-----|-----------|
| `event` | text | required |
| `email` | email | required |
| `ipAddress` | text | optional |
| `userAgent` | text | optional |
| `success` | bool | required |
| `reason` | text | optional |
| `metadata` | json | optional |

**API Rules:**
- List: только админ
- View: только админ
- Create: разрешить всем
- Update: только админ
- Delete: только админ

---

### 📊 Коллекция: `rate_limits`

Для защиты от спама:

| Поле | Тип | Настройки |
|------|-----|-----------|
| `key` | text | required, unique |
| `count` | number | required |
| `windowStart` | date | required |
| `expiresAt` | date | required |

**API Rules:**
- List: только админ
- View: только админ
- Create: разрешить всем
- Update: разрешить всем
- Delete: разрешить всем

---

### 3. Настройка Resend

1. Зарегистрируйтесь на [resend.com](https://resend.com)
2. Создайте API ключ
3. Добавьте и верифицируйте ваш домен (или используйте тестовый домен)

### 4. Конфигурация переменных окружения

Создайте файл `.env.local` (скопируйте из `.env.example`):

```bash
cp .env.example .env.local
```

Заполните переменные:

```env
NEXT_PUBLIC_POCKETBASE_URL=http://127.0.0.1:8090
RESEND_API_KEY=re_ваш_ключ_здесь
NEXT_PUBLIC_FROM_EMAIL=noreply@yourdomain.com
NEXT_PUBLIC_SUPPORT_EMAIL=support@yourdomain.com
```

### 5. Запуск проекта

```bash
# Режим разработки
npm run dev

# Или
yarn dev

# Или
bun dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🔐 Безопасность

### Реализованные меры безопасности:

✅ **Хеширование кодов** - коды верификации хранятся в хешированном виде  
✅ **Rate Limiting** - ограничение попыток отправки кодов  
✅ **Блокировка по попыткам** - автоблокировка после 5 неверных попыток  
✅ **Срок действия кодов** - коды действительны 15 минут  
✅ **Логирование событий** - все действия записываются в security_logs  
✅ **IP трекинг** - запись IP адресов для аудита  
✅ **User Agent трекинг** - запись браузера/устройства  
✅ **HTTPS only** - в продакшене используйте только HTTPS  
✅ **CSP заголовки** - Content Security Policy  
✅ **Валидация входных данных** - проверка всех данных от пользователя  

### Рекомендации для продакшена:

1. **Используйте HTTPS** - обязательно для продакшена
2. **Настройте CORS** - ограничьте доступ к API
3. **Резервное копирование** - регулярно делайте бэкапы PocketBase
4. **Мониторинг** - следите за security_logs
5. **Обновления** - регулярно обновляйте зависимости
6. **Environment Variables** - храните секреты в безопасности

## 📁 Структура проекта

```
slashvpn-complete/
├── src/
│   ├── app/                      # Next.js App Router страницы
│   │   ├── register/            # Страница регистрации
│   │   ├── verify-email/        # Страница верификации
│   │   ├── login/               # Страница входа
│   │   ├── dashboard/           # Личный кабинет (защищено)
│   │   └── page.tsx             # Главная страница
│   ├── components/              # React компоненты
│   │   ├── auth/                # Компоненты аутентификации
│   │   │   ├── RegistrationForm.tsx
│   │   │   ├── EmailVerificationForm.tsx
│   │   │   ├── LoginForm.tsx
│   │   │   └── LoginVerificationForm.tsx
│   │   ├── ui/                  # UI компоненты
│   │   └── ...                  # Другие компоненты
│   ├── lib/                     # Утилиты и конфигурация
│   │   ├── pocketbase.ts       # PocketBase клиент
│   │   ├── email.ts            # Resend email сервис
│   │   ├── utils.ts            # Общие утилиты
│   │   └── validation.ts       # Валидация данных
│   ├── services/               # Бизнес-логика
│   │   └── auth.service.ts     # Сервис аутентификации
│   └── types/                  # TypeScript типы
│       └── auth.types.ts       # Типы для аутентификации
├── .env.example                # Пример переменных окружения
├── .env.local                  # Ваши переменные (не в git)
├── package.json                # Зависимости проекта
└── README.md                   # Эта инструкция
```

## 🎯 Основные страницы

### Регистрация
`/register` - Форма регистрации с email, именем и паролем

### Верификация Email
`/verify-email` - Ввод 6-значного кода из письма

### Вход
`/login` - Passwordless вход по email + код

### Личный Кабинет
`/dashboard` - Защищенная страница с информацией о пользователе

## 🧪 Тестирование

### Тестовая регистрация:

1. Откройте `/register`
2. Заполните форму (используйте реальный email для получения кода)
3. Проверьте почту и введите код
4. Вы будете перенаправлены в `/dashboard`

### Тестовый вход:

1. Откройте `/login`
2. Введите ваш email
3. Получите код на почту
4. Введите код для входа

## 🐛 Отладка

### Проблемы с email:

Если письма не приходят:
1. Проверьте `RESEND_API_KEY` в `.env.local`
2. Убедитесь что домен верифицирован в Resend
3. Проверьте папку "Спам"
4. Посмотрите консоль - в dev режиме коды выводятся в консоль

### Проблемы с PocketBase:

Если не работает регистрация:
1. Убедитесь что PocketBase запущен (`./pocketbase serve`)
2. Проверьте `NEXT_PUBLIC_POCKETBASE_URL` в `.env.local`
3. Проверьте что все коллекции созданы
4. Проверьте API Rules в коллекциях

### Логи безопасности:

Посмотрите логи в PocketBase:
1. Откройте `http://127.0.0.1:8090/_/`
2. Перейдите в Collections > security_logs
3. Изучите события для отладки

## 📦 Продакшн развертывание

### 1. Подготовка:

```bash
# Соберите проект
npm run build

# Проверьте сборку
npm run start
```

### 2. PocketBase на сервере:

```bash
# Загрузите PocketBase на сервер
scp pocketbase user@server:/app/

# Запустите как сервис
./pocketbase serve --http="0.0.0.0:8090"
```

### 3. Next.js деплой:

- **Vercel** (рекомендуется): автоматический деплой из GitHub
- **Docker**: создайте Dockerfile и разверните в контейнере
- **VPS**: используйте PM2 для запуска Next.js

### 4. Переменные окружения:

Установите переменные на сервере:
- `NEXT_PUBLIC_POCKETBASE_URL` - публичный URL вашего PocketBase
- `RESEND_API_KEY` - ваш production ключ Resend
- Остальные переменные из `.env.example`

## 💡 Дополнительные возможности (TODO)

- [ ] Двухфакторная аутентификация (2FA)
- [ ] OAuth (Google, GitHub)
- [ ] Восстановление пароля
- [ ] Изменение email
- [ ] Удаление аккаунта
- [ ] Сессии и устройства
- [ ] Push уведомления
- [ ] Аватары пользователей

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте этот README
2. Посмотрите логи в консоли браузера
3. Проверьте логи PocketBase
4. Проверьте security_logs в PocketBase

## 📝 Лицензия

MIT

## 🙏 Благодарности

- [Next.js](https://nextjs.org)
- [PocketBase](https://pocketbase.io)
- [Resend](https://resend.com)
- [TailwindCSS](https://tailwindcss.com)

---

**Сделано с ❤️ для безопасной аутентификации**
