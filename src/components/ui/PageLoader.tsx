'use client';

// PageLoader — kept for API compatibility.
// Navigation transitions now use blur effect (BlurWrapper in LayoutWrapper).
// This file intentionally left as a no-op export.

export function PageLoader() {
  return null;
}
